# ADR-0002 — Use TypeScript

## Status
Accepted

## Decision
Use TypeScript with strict mode across backend, workers, shared packages and web application.

## Consequences
Shared contracts and domain types can be reused while keeping compile-time guarantees.
