{
  "name": "@cult/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "NEXT_TELEMETRY_DISABLED=1 next dev -p ${WEB_PORT:-3000}",
    "build": "NEXT_TELEMETRY_DISABLED=1 next build",
    "start": "NEXT_TELEMETRY_DISABLED=1 next start -p ${WEB_PORT:-3000}",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "@cult/config": "workspace:*",
    "@cult/domain": "workspace:*",
    "next": "^16.3.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "maplibre-gl": "6.6.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5"
  }
}
